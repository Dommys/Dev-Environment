image:
  repository: bestiee/vue-app
  tag: "d3f2744f42258c8bb5522172e4f41448e24eaff1"
  pullPolicy: IfNotPresent

replicaCount: 2

container:
  port: 80

service:
  type: LoadBalancer     # matches your prod style
  port: 80

ingress:
  enabled: false         # or enable + set a real host if you prefer Ingress
  # ...your existing prod values...
extraContainers:
  - name: api
    image: yourrepo/your-api:1.0.0
    ports:
      - name: api
        containerPort: 3000
service:
  port: 80
  extraPorts:
    - name: api
      port: 3000


